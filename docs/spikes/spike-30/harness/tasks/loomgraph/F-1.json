{
  "task_id": "loomgraph-F-1",
  "fixture": "loomgraph",
  "task_class": "F",
  "subtype": "F1",
  "prompt": "Which production function calls `TopologyAnalyzer.analyze()`? Be specific — there are several analyzer classes in this codebase with a method named `analyze`, but I'm asking about the topology one specifically. Return one qualified entity name per line.",
  "ground_truth": {
    "expected": [
      "_async_topology"
    ],
    "must_mention": [],
    "misresolution_traps": [
      "_async_debt",
      "_async_overview",
      "_async_impact",
      "_async_compare",
      "_async_similar",
      "_async_deps"
    ],
    "notes": "Class F1 — name collision / wrong-class resolution.\n\nThe `analyze` method exists on at least 7 analyzer classes: TopologyAnalyzer, DebtAnalyzer, OverviewAnalyzer, ImpactAnalyzer, CompareAnalyzer, SimilarAnalyzer, DepsAnalyzer. Each is invoked by exactly one cli `_async_<name>` helper.\n\nGround truth: `_async_topology` is the unique production callsite of TopologyAnalyzer.analyze. Verified: src/loomgraph/cli/_analysis.py:237-243 instantiates TopologyAnalyzer and calls .analyze().\n\nMisresolution traps: the 6 OTHER `_async_*` functions that call OTHER analyzer's .analyze(). If Path B returns any of these as a caller of TopologyAnalyzer.analyze, that's a precision failure — the entity exists but it's the wrong target.\n\nThe loomgraph CLI graph treats `analyze` as a method on the qualified class — `TopologyAnalyzer.analyze` should resolve specifically. But if the agent uses unqualified `analyze` searches or follows `.analyze()` callees without checking class context, it could surface the wrong _async_* function.\n\nScoring: correctness 1.0 if `_async_topology` returned AND no misresolution_traps in answer; 0.5 if `_async_topology` returned but ≤1 trap; 0.0 otherwise. Misresolution_count = count of trap entities in answer."
  },
  "rationale": "Class F1 — graph precision under name collision. 7 analyzer classes all have `analyze()`; specific call site for TopologyAnalyzer.analyze is _async_topology only. Tests whether the path's query mechanism qualifies correctly.",
  "sources": [
    "src/loomgraph/cli/_analysis.py",
    "src/loomgraph/core/topology.py"
  ]
}
