{
  "task_id": "loomgraph-A-3",
  "fixture": "loomgraph",
  "task_class": "A",
  "subtype": "A1",
  "prompt": "I'm changing the signature of `inject_parse_result` in `loomgraph.core.injector`. List every entity in this codebase that would be affected — direct callers plus their callers (depth 2). Return one qualified entity name per line. Function names only — no source paths, no explanations.",
  "ground_truth": {
    "expected": [
      "index_file",
      "index_repository",
      "inject_parse_results_batch"
    ],
    "must_mention": [],
    "misresolution_traps": [],
    "notes": "Small-set Class A test. inject_parse_result has exactly 3 direct callers, all sibling functions in the same indexer/injector layer. None of them have internal callers — they're all exported as the public loomgraph.core SDK API surface for users embedding loomgraph in their own code.\n\nDepth-2 set = depth-1 set (3 entities). The agent should recognise that further graph traversal returns 0 results and stop, not pad the answer.\n\nVerified empirically: `loomgraph graph inject_parse_result --direction callers --depth 1` returns 3; each of those has 0 internal callers when queried again."
  },
  "rationale": "Class A1 impact analysis with intentionally small ground-truth set (3 entities). Tests whether Path B's tool-using agent correctly stops at the boundary instead of padding the answer with hallucinations when more graph calls return empty. Path A may do better here because 3 entities is small enough to fit in README prose. Differential between paths should be smaller on this task than on A-1 or A-2 — establishes the scale-dependence of the discrimination.",
  "sources": [
    "src/loomgraph/core/injector.py",
    "src/loomgraph/core/indexer.py"
  ]
}
