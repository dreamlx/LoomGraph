{
  "task_id": "loomgraph-E-4",
  "fixture": "loomgraph",
  "task_class": "E",
  "subtype": "E4",
  "prompt": "Which production functions actually call `SqliteGraphStore.create_entity` at runtime in this codebase? Note: this is a polymorphic method override of `GraphStore.create_entity` (ABC). Return one qualified entity name per line. Function names only — no source paths, no explanations.",
  "ground_truth": {
    "expected": [
      "inject_parse_result",
      "SqliteGraphStore.insert_custom_kg",
      "_async_index_pipeline",
      "_async_warm_update",
      "_async_workspace_delete"
    ],
    "must_mention": [],
    "misresolution_traps": [],
    "notes": "Polymorphic-dispatch ground truth. At runtime, `SqliteGraphStore.create_entity` is called via:\n- `inject_parse_result(client, ...)` where client is typed `GraphStore` — actual subclass is SqliteGraphStore in production paths (factory.create_graph_store returns it)\n- `SqliteGraphStore.insert_custom_kg` calls `self.create_entity` internally for each row\n- `_async_index_pipeline` and `_async_warm_update` build the store via `create_graph_store()` then call `store.insert_custom_kg` which fans out to create_entity\n- `_async_workspace_delete` doesn't actually call create_entity — removed from set after re-check\n\nKnown failure mode (THIS is the test): `loomgraph graph SqliteGraphStore.create_entity` returns 0 callers because the AST extractor resolves call sites to `GraphStore.create_entity` or unbound `client.create_entity`, not the concrete subclass method. Path B (loomgraph CLI) will likely return empty / wrong. Path A (README_AI) may recover via prose mentioning 'inject_parse_result writes module + symbol entities via the store abstraction'.\n\nThis IS the gating signal for #102 graph-export schema固化: if Path B's only response is 'no callers found' without a polymorphism caveat, schema固化 with `provenance=AST` would authoritatively transmit wrong data."
  },
  "rationale": "Class E4 adversarial recall — polymorphic / ABC dispatch. Tests the failure mode discovered during A-1 calibration: graph misses callers when target is behind an ABC. Critical for #102 — if Path B silently returns 0 callers, the export schema would authoritatively transmit incompleteness with no caveat field.",
  "sources": [
    "src/loomgraph/storage/sqlite_store.py",
    "src/loomgraph/core/injector.py",
    "src/loomgraph/cli/_indexing.py"
  ]
}
