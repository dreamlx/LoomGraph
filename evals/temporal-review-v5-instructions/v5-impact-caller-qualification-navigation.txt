请审查一个历史代码变更，并列出当前源码中需要复核的实现位置。

比较别名：v5-base..v5-head
backend：codeindex

位置必须使用仓库相对 path 与 Python qualname。qualname 只可由函数、异步函数和类名以 `.` 连接；不要使用 `::`、模块全限定名、变量名或解释性短语。

decision.boundary 只能是 comparison_not_observed、content_comparison_available 或 content_comparison_unavailable。review_loci 最多列出三个位置。

不要逐值转录工具返回的比较对象、ref、backend、provisioning、status、reason、availability 或证据类别；只用闭合的 decision.boundary 表示比较边界。

只输出一个 JSON 对象，顶层字段只能是 decision、review_loci：
{
  "decision": {"boundary": "...", "rationale": "..."},
  "review_loci": [
    {"path": "...", "qualname": "...", "rationale": "..."}
  ]
}
