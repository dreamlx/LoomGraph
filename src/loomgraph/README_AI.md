<!-- codeindex navigation index — agent: drill into source via Read/Grep for precise mechanism; do not treat this as final word. -->
<!-- Generated by codeindex (detailed) at 2026-07-04T00:39:52.026106 -->

# loomgraph

## Overview

- **Files**: 65
- **Symbols**: 327

## Files

### __init__.py
_LoomGraph: Enterprise Code Intelligence Engine

Code understanding + retrieval over a local SQLite + sqlite-vec knowledge
graph, with pluggable OpenAI_

### __init__.py
_Chunking module: AST-aware code chunking using codeindex._

### __init__.py
_CLI module: Command-line interface for LoomGraph._

### _analysis.py
_CLI commands for impact analysis, deps, overview, topology, and check._

**Functions:**
- `def impact(target: str, staged: bool, base: str | None, depth: int, file_path: str | None, workspace: str | None) -> None`
- `def _async_impact(
    target: str,
    staged: bool,
    base: str | None,
    depth: int,
    file_path: str | None,
    workspace: str | None = None,
) -> dict[str, Any]`
- `def deps(depth: int, workspace: str | None) -> None`
- `def _async_deps(depth: int, workspace: str | None = None) -> dict[str, Any]`
- `def overview(depth: int, workspace: str | None, no_summary: bool) -> None`
- `def _async_overview(
    depth: int, workspace: str | None = None, no_summary: bool = False
) -> dict[str, Any]`
- `def topology(hub_threshold: int, god_threshold: int, module: str | None, workspace: str | None) -> None`
- `def _async_topology(
    hub_threshold: int,
    god_threshold: int,
    module: str | None = None,
    workspace: str | None = None,
) -> dict[str, Any]`
- `def check(repo_path: str, workspace: str | None) -> None`
- `def _async_check(
    repo_path: str,
    workspace: str | None = None,
) -> dict[str, Any]`
- `def git_metrics(path: str, since: str, output: str | None) -> None`
- `def trends(entity: str, metric: str, months: int, workspace: str | None) -> None`

### _backfill.py
_CLI command: embed-backfill — populate vec_node_descriptions for
an already-indexed workspace.

EPIC-015 Phase 3 (#70). For a workspace that has entit_

**Functions:**
- `def embed_backfill(workspace: str | None) -> None`
- `def _async_embed_backfill(
    workspace: str | None = None,
) -> dict[str, Any]`

### _common.py
_Common helpers shared across CLI submodules._

**class** `class ErrorCode`
> Structured error codes for AI Agent parsing.

**Functions:**
- `def _setup_logging(verbose: bool, quiet: bool) -> None`
- `def resolve_workspace_with_fallback(
    workspace: str,
    store: Any,  # GraphStore
    allow_fallback: bool = True,
) -> str`
- `def prepare_workspace_store(
    workspace: str | None = None,
) -> tuple[str, Any]`
- `def create_llm_client_for_workspace(workspace: str | None = None) -> Any`
- `def output_success(data: dict[str, Any]) -> None`
- `def output_error(
    code: str,
    message: str,
    suggestion: str | None = None,
    docs: str | None = None,
    data: dict[str, Any] | None = None,
) -> None`
- `def output_partial_error(
    code: str,
    message: str,
    suggestions: list[str],
    data: dict[str, Any],
) -> None`

### _debt.py
_CLI commands for technical debt analysis._

**Functions:**
- `def debt(
    codeindex_data: Path | None,
    output_format: str,
    workspace: str | None,
    module: str | None,
    skip_topology: bool,
    with_git: bool,
    git_since: str,
) -> None`
- `def _async_debt(
    codeindex_data_path: Path | None,
    output_format: str,
    workspace: str | None,
    module: str | None,
    skip_topology: bool,
    with_git: bool,
    git_since: str,
) -> dict[str, Any]`
- `def _format_markdown(report: dict[str, Any]) -> dict[str, Any]`
- `def _format_console(report: dict[str, Any]) -> dict[str, Any]`

### _deps_check.py
_Dependency check helpers for CLI status command._

**Functions:**
- `def check_codeindex() -> dict[str, Any]`
- `def check_storage(settings: Any) -> dict[str, Any]`
- `def check_embedding(settings: Any) -> dict[str, Any]`

### _hooks.py
_Git hooks management for loomgraph._

**Functions:**
- `def find_git_repo() -> Path`
- `def is_installed(hook_name: str) -> bool`
- `def install_hook(hook_name: str, force: bool = False) -> bool`
- `def uninstall_hook(hook_name: str, restore_backup: bool = True) -> bool`
- `def hooks() -> None`
- `def install(install_all: bool, force: bool) -> None`
- `def uninstall(uninstall_all: bool, no_restore: bool) -> None`
- `def status() -> None`

### _import_export.py
_CLI command: import a codeindex `graph-export` NDJSON artifact.

This wires the codeindex#102 producer to a loomgraph-side consumer
workspace. Mapping_

**Functions:**
- `def import_export(
    artifact: Path,
    workspace: str | None,
    clear: bool,
    dry_run: bool,
) -> None`
- `def _async_import_export(
    artifact: Path,
    workspace: str,
    *,
    clear: bool,
    dry_run: bool,
) -> dict[str, Any]`

### _indexing.py
_CLI commands for indexing and updating the knowledge graph._

**Functions:**
- `def index(repo_path: str, clear: bool, workspace: str | None) -> None`
- `def _async_index(
    entities: list[Any],
    relations: list[Any],
    workspace: str | None,
    clear: bool,
) -> dict[str, Any]`
- `def update(
    since: str,
    workspace: str | None,
    files: str | None,
    embedding_url: str | None,
    use_affected: bool,
) -> None`

### _mcp.py
_CLI command: `loomgraph mcp serve` — start the MCP stdio server.

This is the production entry point referenced from `~/.claude/mcp.json`
(or equivale_

**Functions:**
- `def mcp() -> None`
- `def serve(default_workspace: str | None) -> None`
- `def install_config(path: str | None) -> None`

### _search.py
_CLI commands for find, query, and graph queries._

**class** `class VectorsNotIndexedError(RuntimeError)`
> Workspace has no embedded vectors — semantic search unavailable.

    Raised by `_async_search` when

**Functions:**
- `def find(
    query: str,
    entity_type: str | None,
    workspace: str | None,
    limit: int,
    with_relations: bool,
    depth: int,
) -> None`
- `def search(query: str, entity_type: str | None, workspace: str | None, limit: int) -> None`
- `def _async_find(
    query: str,
    entity_type: str | None = None,
    workspace: str | None = None,
    limit: int = 20,
    with_relations: bool = False,
    depth: int = 1,
) -> dict[str, Any]`
- `def _async_search(
    query: str,
    entity_type: str | None = None,
    workspace: str | None = None,
    limit: int = 20,
) -> dict[str, Any]`
- `def _attach_relations(
    matches: list[dict[str, Any]],
    relations: list[dict[str, Any]],
    depth: int = 1,
) -> None`
- `def _bfs_collect(
    start: str,
    adj: dict[str, list[dict[str, str]]],
    depth: int,
) -> list[dict[str, str]]`
- `def graph(entity_name: str, direction: str, depth: int, relation_type: str, workspace: str | None) -> None`
- `def _async_graph_query(
    entity_name: str,
    direction: str,
    relation_type: str,
    workspace: str | None = None,
) -> dict[str, Any]`

### _setup.py
_CLI commands for status, setup, and utility operations._

**Functions:**
- `def status() -> None`
- `def install_skills() -> None`
- `def version() -> None`

### _workspace.py
_CLI commands for workspace management and cross-workspace analysis._

**Functions:**
- `def workspace() -> None`
- `def workspace_list() -> None`
- `def _async_workspace_list() -> dict[str, Any]`
- `def workspace_info(name: str | None, ws_option: str | None) -> None`
- `def _async_workspace_info(name: str | None, ws_option: str | None) -> dict[str, Any]`
- `def workspace_delete(name: str, yes: bool) -> None`
- `def _async_workspace_delete(name: str) -> dict[str, Any]`
- `def compare(ws1: str, ws2: str) -> None`
- `def _async_compare(ws1: str, ws2: str) -> dict[str, Any]`
- `def similar(entity: str, workspaces: str | None) -> None`
- `def _async_similar(
    entity: str, workspaces: str | None = None
) -> dict[str, Any]`

### main.py
_LoomGraph CLI - AI Agent Friendly Interface.

Design: CLI outputs JSON for machine parsing by AI Agent (Claude Code).
See docs/api/CLI_DESIGN.md for f_

**Functions:**
- `def main(verbose: bool, quiet: bool) -> None`
- `def cli_entry() -> None`

### __init__.py
_Core module: configuration, git, and graph-store data models._

### compare.py
_Cross-workspace comparison.

Compares entities and relations between two workspaces using
entity name matching (Phase 1)._

**class** `class CompareResult`
> Result of cross-workspace comparison.

**class** `class CompareAnalyzer`
> Compares entities and relations between two workspaces.

    Args:
        client1: LightRAG client 

**Methods:**
- `def to_dict(self) -> dict[str, Any]`
- `def analyze(self) -> CompareResult`

**Functions:**
- `def _entity_to_info(entity: dict[str, Any]) -> dict[str, str]`
- `def _relation_key(rel: dict[str, Any]) -> tuple[str, str, str]`
- `def _build_entity_relations(
    relations: list[dict[str, Any]], shared_names: set[str]
) -> dict[str, set[tuple[str, str, str]]]`

### config.py
_Configuration management for LoomGraph.

Configuration priority (highest to lowest):
1. Environment variables (LOOMGRAPH_*)
2. .loomgraph.yaml in curr_

**class** `class ASTExtractionConfig(BaseSettings)`
> AST extraction configuration.

**class** `class SemanticEnhancementConfig(BaseSettings)`
> LLM semantic enhancement configuration (disabled in MVP).

**class** `class IndexingConfig(BaseSettings)`
> Indexing pipeline configuration.

**class** `class EmbeddingConfig(BaseSettings)`
> Embedding provider configuration (EPIC-012 / Phase 6).

    Default off — pipx install gives you a u

**class** `class StorageConfig(BaseSettings)`
> Storage backend selection (EPIC-011 / ADR-013).

    Phase 5: only `sqlite` is supported. The legacy

**class** `class LLMConfig(BaseSettings)`
> LLM provider configuration (EPIC-011 Phase 4 / ADR-013).

    Phase 5: `lightrag` option dropped tog

**class** `class RetrievalConfig(BaseSettings)`
> Retrieval configuration.

**class** `class Settings(BaseSettings)`
> Main application settings.

**class** 

---
_Content truncated due to size limit. See individual module README files for details._
