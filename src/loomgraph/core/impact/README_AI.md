<!-- Generated by codeindex (detailed) at 2026-02-18T22:33:20.181183 -->

# impact

## Overview

- **Files**: 6
- **Symbols**: 50

## Files

### __init__.py
_Impact analysis module for LoomGraph.

This module provides tools for analyzing the impact of code changes
by combining git diff parsing with knowledg_

### analyzer.py
_Impact analyzer using LightRAG knowledge graph._

**class** `class ImpactAnalyzer`
> Analyzes impact of code changes using knowledge graph.

    Combines git diff parsing, symbol extrac

**Methods:**
- `def analyze_commit(self, commit: str = "HEAD") -> ImpactResult`
- `def analyze_staged(self) -> ImpactResult`
- `def analyze_branch_diff(
        self, base: str, head: str = "HEAD"
    ) -> ImpactResult`
- `def _find_callers(
        self, symbols: list[ChangedSymbol]
    ) -> tuple[list[Caller], list[Caller]]`
- `def _query_callers(self, symbol_name: str, depth: int = 1) -> list[dict]`
- `def _parse_caller_response(self, response: str) -> list[dict]`
- `def _identify_affected_modules(
        self,
        symbols: list[ChangedSymbol],
        direct_callers: list[Caller],
        indirect_callers: list[Caller],
    ) -> list[str]`
- `def _file_to_module(self, file_path: str) -> str`
- `def _identify_affected_tests(
        self,
        direct_callers: list[Caller],
        indirect_callers: list[Caller],
    ) -> list[str]`
- `def _is_test_file(self, file_path: str) -> bool`

### extractor.py
_Symbol extraction from changed files._

**class** `class ExtractorError(Exception)`
> Exception raised for extraction errors.

**class** `class ChangedSymbolExtractor`
> Extracts code symbols from changed files.

    Uses codeindex to parse files and identify which symb

**Methods:**
- `def extract_from_files(self, files: list[ChangedFile]) -> list[ChangedSymbol]`
- `def _extract_from_file(self, file: ChangedFile) -> list[ChangedSymbol]`
- `def _extract_from_deleted_file(self, file: ChangedFile) -> list[ChangedSymbol]`
- `def _extract_all_symbols(
        self, file_path: Path, file: ChangedFile, change_type: ChangeType
    ) -> list[ChangedSymbol]`
- `def _extract_modified_symbols(
        self, file_path: Path, file: ChangedFile
    ) -> list[ChangedSymbol]`
- `def _ranges_overlap(
        self, start1: int, end1: int, start2: int, end2: int
    ) -> bool`
- `def _count_changed_lines(
        self, symbol_start: int, symbol_end: int, ranges: list[tuple[int, int]]
    ) -> int`
- `def _run_codeindex(self, file_path: Path) -> list[dict]`

### git_parser.py
_Git diff parsing utilities._

**class** `class GitError(Exception)`
> Exception raised for git command errors.

**class** `class GitDiffParser`
> Parser for git diff output.

    Parses git diff output to extract changed files and line ranges.

**Methods:**
- `def _run_git(self, *args: str) -> str`
- `def _parse_diff(self, diff_output: str) -> list[ChangedFile]`
- `def has_changes(self) -> bool`

### models.py
_Data models for impact analysis._

**class** `class ChangeType(Enum)`
> Type of change detected in a symbol.

**class** `class ChangedFile`
> Represents a file that was changed in a commit.

**class** `class ChangedSymbol`
> Represents a code symbol that was changed.

**class** `class Caller`
> Represents a function/method that calls a changed symbol.

**class** `class RiskAssessment`
> Risk assessment for a set of changes.

**class** `class ImpactResult`
> Complete impact analysis result.

**Methods:**
- `def to_dict(self) -> dict[str, Any]`
- `def to_dict(self) -> dict[str, Any]`
- `def to_dict(self) -> dict[str, Any]`
- `def to_dict(self) -> dict[str, Any]`
- `def to_dict(self) -> dict[str, Any]`

### risk.py
_Risk assessment for code changes._

**class** `class RiskAssessor`
> Assesses risk level of code changes.

    Uses number of callers, affected modules, and module criti

**Methods:**
- `def assess(self, result: ImpactResult) -> RiskAssessment`
- `def assess_from_callers(self, callers: list[Caller]) -> RiskAssessment`
- `def _is_core_module_change(self, symbols: list[ChangedSymbol]) -> bool`
- `def _build_high_risk_reason(
        self,
        total_callers: int,
        is_core_change: bool,
        has_many_affected: bool,
        result: ImpactResult,
    ) -> str`
- `def _build_medium_risk_reason(
        self, total_callers: int, result: ImpactResult
    ) -> str`
- `def _build_low_risk_reason(self, total_callers: int, result: ImpactResult) -> str`
- `def _generate_suggestions(
        self, level: str, result: ImpactResult
    ) -> list[str]`

## Dependencies

- dataclasses
- enum
- json
- loomgraph.core.impact.analyzer
- loomgraph.core.impact.extractor
- loomgraph.core.impact.git_parser
- loomgraph.core.impact.models
- loomgraph.core.impact.risk
- pathlib
- re
- subprocess
- typing
