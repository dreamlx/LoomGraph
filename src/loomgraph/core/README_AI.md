<!-- codeindex navigation index — agent: drill into source via Read/Grep for precise mechanism; do not treat this as final word. -->
<!-- Generated by codeindex (detailed) at 2026-07-03T23:24:04.991875 -->

# core

## Overview

- **Files**: 25
- **Symbols**: 182

## Files

### __init__.py
_Core module: configuration, mapping, injection, and indexing._

### adapter.py
_Adapter to convert codeindex output to LoomGraph models.

This module bridges the gap between codeindex's ParseResult and
LoomGraph's internal ParseRe_

**Functions:**
- `def adapt_parse_result(codeindex_result: CodeindexParseResult) -> ParseResult`

### compare.py
_Cross-workspace comparison.

Compares entities and relations between two workspaces using
entity name matching (Phase 1)._

**class** `class CompareResult`
> Result of cross-workspace comparison.

**class** `class CompareAnalyzer`
> Compares entities and relations between two workspaces.

    Args:
        client1: LightRAG client 

**Methods:**
- `def to_dict(self) -> dict[str, Any]`
- `def analyze(self) -> CompareResult`

**Functions:**
- `def _entity_to_info(entity: dict[str, Any]) -> dict[str, str]`
- `def _relation_key(rel: dict[str, Any]) -> tuple[str, str, str]`
- `def _build_entity_relations(
    relations: list[dict[str, Any]], shared_names: set[str]
) -> dict[str, set[tuple[str, str, str]]]`

### config.py
_Configuration management for LoomGraph.

Configuration priority (highest to lowest):
1. Environment variables (LOOMGRAPH_*)
2. .loomgraph.yaml in curr_

**class** `class ASTExtractionConfig(BaseSettings)`
> AST extraction configuration.

**class** `class SemanticEnhancementConfig(BaseSettings)`
> LLM semantic enhancement configuration (disabled in MVP).

**class** `class IndexingConfig(BaseSettings)`
> Indexing pipeline configuration.

**class** `class EmbeddingConfig(BaseSettings)`
> Embedding provider configuration (EPIC-012 / Phase 6).

    Default off — pipx install gives you a u

**class** `class StorageConfig(BaseSettings)`
> Storage backend selection (EPIC-011 / ADR-013).

    Phase 5: only `sqlite` is supported. The legacy

**class** `class LLMConfig(BaseSettings)`
> LLM provider configuration (EPIC-011 Phase 4 / ADR-013).

    Phase 5: `lightrag` option dropped tog

**class** `class RetrievalConfig(BaseSettings)`
> Retrieval configuration.

**class** `class Settings(BaseSettings)`
> Main application settings.

**class** `class ConfigSchemaError(RuntimeError)`
> User-facing config-load failure (clearer than a pydantic stack trace).

**Functions:**
- `def load_yaml_config() -> dict[str, Any]`
- `def flatten_dict(d: dict[str, Any], parent_key: str = "", sep: str = "__") -> dict[str, Any]`
- `def _remove_env_overrides(
    config: dict[str, Any], key_parts: list[str] | None = None
) -> dict[str, Any]`
- `def _format_validation_error(exc: Exception) -> str`
- `def reset_settings() -> None`

### debt_analyzer.py
_Technical Debt Analyzer

Integrates codeindex static analysis data with LoomGraph graph topology
to provide multi-dimensional technical debt scoring.
_

**class** `class CodeindexData`
> Normalized codeindex output data.

    This class represents the imported and normalized data from c

**class** `class DebtIssue`
> Represents a single technical debt issue.

    Maps to the 'issues' array in the debt report format.

**class** `class DebtAnalyzer`
> Technical Debt Analyzer

    Combines codeindex static analysis with LoomGraph graph topology
    to

**Methods:**
- `def import_codeindex_data(self, raw_data: dict[str, Any]) -> CodeindexData`
- `def _enrich_giant_functions(
        self, functions: list[dict[str, Any]]
    ) -> list[dict[str, Any]]`
- `def _normalize_test_smells(
        self, smells: list[dict[str, Any]]
    ) -> list[dict[str, Any]]`
- `def aggregate_breakdown(
        self, file_reports: list[dict[str, Any]], path: str
    ) -> dict[str, int]`
- `def analyze(
        self,
        codeindex_data: dict[str, Any] | None = None,
        module: str | None = None,
        with_git: bool = False,
        git_since: str = "3 months",
    ) -> dict[str, Any]`
- `def _analyze_codeindex_issues(self, data: CodeindexData) -> None`
- `def _analyze_topology_issues(self, module: str | None = None) -> int`
- `def _lookup_maintainability(self, data: CodeindexData, path: str) -> float`
- `def _calculate_maintainability_score(
        self, data: CodeindexData | None
    ) -> float`
- `def _calculate_overall_health(
        self,
        topology_score: int = 100,
        git_score: int | None = None,
        maintainability_score: float = 100.0,
    ) -> dict[str, Any]`
- `def _issue_to_dict(self, issue: DebtIssue) -> dict[str, Any]`
- `def _analyze_git_issues(self, git_metrics: Any) -> int`

_... and 2 more symbols_

### deps.py
_Module dependency analysis.

Analyzes cross-module dependencies by querying the knowledge graph
and grouping entities by their file path modules._

**class** `class DepsResult`
> Result of dependency analysis.

**class** `class DepsAnalyzer`
> Analyzes module-level dependencies from the knowledge graph.

    Args:
        client: GraphStore i

**Methods:**
- `def to_dict(self) -> dict[str, Any]`
- `def analyze(self) -> DepsResult`

**Functions:**
- `def extract_module(file_path: str, depth: int) -> str`

### embedding_pipeline.py
_Embedding attachment for entity dicts (settings-gated).

Lives in `core/` (not `cli/`) so the graph-export ingestion pipeline
(`core/graph_export_inge_

**Functions:**
- `def maybe_embed_entities(entities: list[dict[str, Any]]) -> int`

### git.py
_Git integration for detecting changed files.

This module provides utilities for detecting file changes in git repositories,
used by the Warm Update s_

**class** `class GitError(Exception)`
> Exception raised when git operations fail.

**Functions:**
- `def is_git_repository(path: Path | str = ".") -> bool`

### git_metrics.py
_Git metrics analyzer for technical debt analysis.

This module provides GitMetricsAnalyzer for detecting:
- Hotspots: High-frequency change files (sys_

**class** `class GitMetricsAnalyzer`
> Analyze git history for technical debt metrics.

**Methods:**
- `def analyze(self) -> GitMetricsResult`
- `def _detect_hotspots(self, file_metrics: dict[str, FileMetrics]) -> list[Hotspot]`
- `def _calculate_hotspot_score(self, change_freq: int, churn: int) -> int`
- `def _detect_bus_factor(
        self,
        file_metrics: dict[str, FileMetrics],
        ownership_threshold: float = 0.7,
    ) -> list[BusFactor]`

### git_parser.py
_Git log parser for extracting commit history metrics.

This module provides GitLogParser for parsing git log output
and aggregating file-level metrics_

**class** `class GitLogParser`
> Parse git log to extract file-level metrics.

**Methods:**
- `def parse_commits(self, since: str = "3 months") -> list[dict]`
- `def _parse_log_output(self, output: str) -> list[dict]`
- `def _is_bug_fix(self, message: str) -> bool`
- `def _aggregate_file_metrics(self, commits: list[dict]) -> dict[str, FileMetrics]`

### graph_export_ingest.py
_Shared graph-export ingestion pipeline (#66).

Retires the legacy `codeindex scan --output json` path: both `loomgraph index`
and `loomgraph update` c_

**class** `class GraphExportError(RuntimeError)`
> `codeindex graph-export` failed, timed out, or produced no readable output.

**Functions:**
- `def run_graph_export(
    repo: Any,
    *,
    timeout: int = 600,
) -> tuple[list[EntityData], list[RelationData], ImportSummary]`
- `def _emit(
    on_progress: ProgressFn | None,
    phase: str,
    n_entities: int,
    n_relations: int,
) -> None`
- `def ingest(
    entities: list[EntityData],
    relations: list[RelationData],
    store: Any,
    *,
    clear: bool,
    on_progress: ProgressFn | None = None,
) -> dict[str, Any]`

### __init__.py
_Impact analysis module for LoomGraph.

This module provides tools for analyzing the impact of code changes
by combining git diff parsing with knowledg_

### analyzer.py
_Impact analyzer using the knowledge graph (deterministic CALLS traversal)._

**class** `class ImpactAnalyzer`
> Analyzes impact of code changes using knowledge graph.

    Combines git diff parsing, symbol extrac

**Methods:**
- `def analyze_commit(self, commit: str = "HEAD") -> ImpactResult`
- `def analyze_staged(self) -> ImpactResult`
- `def analyze_branch_diff(
        self, base: str, head: str = "HEAD"
    ) -> ImpactResult`
- `def _find_callers(
        self, symbols: list[ChangedSymbol]
    ) -> tuple[list[Caller], list[Caller]]`
- `def _query_callers(self, symbol_name: str, depth: int = 1) -> list[dict]`
- `def _parse_caller_response(self, response: str) -> list[dict]`
- `def _identify_affected_modules(
        self,
        symbols: list[ChangedSymbol],
        direct_callers: list[Caller],
        indirect_callers: list[Caller],
    ) -> list[str]`
- `def _file_to_module(self, file_path: str) -> str`
- `def _identify_affected_tests(
        self,
        direct_callers: list[Caller],
        indirect_callers: list[Caller],
    ) -> list[str]`
- `def _is_test_file(self, file_path: str) -> bool`

### extractor.py
_Symbol extraction from changed files._

**class** `class ExtractorError(Exception)`
> Exception raised for extraction errors.

**class** `class ChangedSymbolExtractor`
> Extracts code symbols from changed files.

    Uses codeindex to parse files and identify which symb

**Methods:**
- `def extract_from_files(self, files: list[ChangedFile]) -> list[ChangedSymbol]`
- `def _extract_from_file(self, file: ChangedFile) -> list[ChangedSymbol]`
- `def _extract_from_deleted_file(self, file: ChangedFile) -> list[ChangedSymbol]`
- `def _extract_all_symbols(
        self, file_path: Path, file: ChangedFile, change_type: ChangeType
    ) -> list[ChangedSymbol]`
- `def _extract_modified_symbols(
        self, file_path: Path, file: ChangedFile
    ) -> list[ChangedSymbol]`
- `def _ranges_overlap(
        self, start1: int, end1: int, start2: int, end2: int
    ) -> bool`
- `def _count_changed_lines(
        self, sym

---
_Content truncated due to size limit. See individual module README files for details._
