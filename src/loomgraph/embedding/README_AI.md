<!-- Generated by codeindex (detailed) at 2026-02-18T22:33:20.179084 -->

# embedding

## Overview

- **Files**: 3
- **Symbols**: 12

## Files

### __init__.py
_Embedding module for code vectorization._

### base.py
_Abstract base class for embedding clients._

**class** `class EmbeddingResult`
> Result of an embedding operation.

**class** `class EmbeddingClient(ABC)`
> Abstract base class for embedding clients.

    Implementations should handle:
    - Batching for la

### jina.py
_Jina Code V2 embedding client.

Uses Hugging Face Text Embeddings Inference (TEI) server or Jina API.
Optimized for code with 8K context window._

**class** `class JinaEmbeddingClient(EmbeddingClient)`
> Embedding client for Jina Code V2 via TEI or Jina API.

    Supports two modes:
    1. Local TEI ser

**Methods:**
- `def _get_client(self) -> httpx.AsyncClient`
- `def close(self) -> None`
- `def embed(self, texts: list[str]) -> EmbeddingResult`
- `def embed_single(self, text: str) -> list[float]`
- `def _embed_batch(self, texts: list[str]) -> dict[str, Any]`
- `def embed_with_retry(
        self,
        texts: list[str],
        max_retries: int = 3,
        retry_delay: float = 1.0,
    ) -> EmbeddingResult`

## Dependencies

- .base
- abc
- asyncio
- dataclasses
- httpx
- logging
- loomgraph.core.config
- loomgraph.embedding.base
- loomgraph.embedding.jina
- typing
